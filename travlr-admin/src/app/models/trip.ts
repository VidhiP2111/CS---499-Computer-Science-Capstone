//Updating as per the datatype of a variables
export class Trip {
  _id!: string;
  code!: string;
  name!: string;
  length!: number;
  start!: string;
  resort!: string;
  perPerson!: number;
  image!: string;
  description!: string;
}